import * as vscode from 'vscode';
import { AgentLoop, AgentMode } from '../agent/agentLoop';
import { MemoryStore } from '../memory/memoryStore';
import { ContextProvider } from '../context/contextProvider';
import { ModelRegistry } from '../ai/modelRegistry';
import { HybridModelRuntime } from '../runtime/hybridModelRuntime';
type RuntimeChanged = (discoverModels?: boolean) => Promise<void> | void;
export declare class ChatPanelProvider implements vscode.WebviewViewProvider {
    private readonly extensionUri;
    private readonly agent;
    private readonly runtime;
    private readonly modelRegistry;
    private readonly onRuntimeChanged;
    static readonly viewType = "sinter.chatPanel";
    private webviewView?;
    private memory;
    private contextProvider;
    private workspaceStatsCache?;
    constructor(extensionUri: vscode.Uri, agent: AgentLoop, runtime: HybridModelRuntime, modelRegistry: ModelRegistry, onRuntimeChanged: RuntimeChanged);
    setMemory(memory: MemoryStore): void;
    setContextProvider(contextProvider: ContextProvider): void;
    resolveWebviewView(view: vscode.WebviewView): void;
    postMessage(msg: unknown): void;
    sendPrefilled(text: string, mode?: AgentMode): void;
    sendRuntimeStatus(): Promise<void>;
    private handleSend;
    private handleMessage;
    private sendConversationList;
    private loadConversation;
    private deleteConversation;
    private discoverModels;
    private selectModelFromPanel;
    private resolveRequestedMode;
    private inferUnifiedMode;
    private buildRuntimeStatus;
    private computeConfidence;
    private getWorkspaceStats;
    private scanWorkspace;
    private isSourceLike;
    private countLines;
    private detectPackageManager;
    private getPackageVersion;
    private getHtml;
}
export {};
